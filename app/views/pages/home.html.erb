<div summary="For signed-in users">  
        Enter your flight information on the right to find the price history for that flight.<br /><br />        <font class="note">Beta Limitations:</font> Only Jet Blue hubs for one-way flights within the next month<br />
        Note that all fields must exactly match flight information
</div>

<% form_tag flights_path, :method => 'get' do %>
<p>  
  <!--<%= label_tag 'airline' %>
  <%= text_field :airline, params[:airline]%> <br />-->  
  <%= label_tag 'origin' %>
  <%= select_tag(:origin, options_for_select([["Boston", "BOS"],["Baltimore", "BWI"]]))%><br />
  <%= label_tag 'destination' %>
  <%= select_tag(:destination, options_for_select([["Boston", "BOS"],["Baltimore", "BWI"]]))%><br />
  <%= label_tag 'date' %>
  <%= select_date Date.today, :prefix => :flightDate %> <br />
  <%= label_tag 'departure time' %>
  <%= select_time Time.now, :prefix => :departs %> <br />
  <%= submit_tag "Fetch", :name => nil %>  
</p>
<% end %>