<div>  
        Enter your flight information on the right to find the price history for that flight.<br /><br />        <font class="note">Beta Limitations:</font> Only Jet Blue and AirTran hubs for one-way flights within the next month<br /><br />
</div>

<script>
	$(function() {
		$( "#flight_date" ).datepicker({ minDate: -200, maxDate: "+1M +10D", showOn: "button",
			buttonImage: "images/calendar.gif",
			buttonImageOnly: true });
	});
</script>


<% form_tag flights_path, :method => 'get' do %>
<p>  
  <!--<%= label_tag 'airline' %>
  <%= select_tag(:airline, options_for_select([["AirTran", "AT"], ["Jet Blue", "JB"]]))%><br />-->
  <%= label_tag 'origin' %>
  <%= select_tag(:origin, options_for_select([["Boston", "BOS"], ["Baltimore", "BWI"]]))%><br />
  <%= label_tag 'destination' %>
  <%= select_tag(:destination, options_for_select([["Boston", "BOS"],["Baltimore", "BWI"]]))%><br />
  <%= label_tag 'date' %>
  <%= text_field_tag(:flight_date) %><br />
  <%= submit_tag "Fetch", :name => nil %>
  
</p>
<% end %>
